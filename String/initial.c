#include<stdio.h>
main(){
	char nome[4],sobrenome[4];
	//gets(vet);
	scanf("%s %s",nome,sobrenome);
	printf("nome: %s\n",nome); 
	printf("sobrenome: %s\n",sobrenome);
	// fgets (str, 256, stdin); (Variavel, limite, entrada por teclado)
}
